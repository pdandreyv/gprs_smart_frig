<?php
echo "asdf1"; exit;
//error_reporting(E_ALL);
//ini_set("display_errors", 1);


//$host='localhost'; // имя хоста (уточняется у провайдера)
//$database='baltika'; // имя базы данных, которую вы должны создать
//$user='root'; // заданное вами имя пользователя, либо определенное провайдером
//$pswd='Innovation2cthdth'; // заданный вами пароль
echo "asfd"; exit;
/*$host='localhost'; // имя хоста (уточняется у провайдера)
$database='gprs'; // имя базы данных, которую вы должны создать
$user='root'; // заданное вами имя пользователя, либо определенное провайдером
$pswd='pdandrey'; // заданный вами пароль

$dbh = mysql_connect($host, $user, $pswd) or die("Не могу соединиться с MySQL.");
mysql_select_db($database) or die("Не могу подключиться к базе.");


    $query = "UPDATE `icebox` SET status=1 WHERE status=2 AND id NOT IN (SELECT icebox_id FROM `data` WHERE created_at > NOW() - INTERVAL 2 HOUR GROUP BY icebox_id)";
    $res = mysql_query($query);
    
mysql_close($dbh);
*/
?>